'use client'; import * as React from 'react';
type TabsCtx={value:string,setValue:(v:string)=>void}; const Ctx=React.createContext<TabsCtx|null>(null);
export interface TabsProps{defaultValue?:string;value?:string;onValueChange?:(v:string)=>void;children:React.ReactNode;className?:string;}
export function Tabs({defaultValue='',value,onValueChange,children,className}:TabsProps){const[internal,setInternal]=React.useState(defaultValue||'');const isCtrl=value!==undefined;const val=isCtrl?(value as string):internal;const setVal=(v:string)=>{if(!isCtrl)setInternal(v);onValueChange?.(v);};return <div className={className}><Ctx.Provider value={{value:val,setValue:setVal}}>{children}</Ctx.Provider></div>;}
export function TabsList({children,className='' }:{children:React.ReactNode;className?:string}){return <div className={`flex gap-2 ${className}`}>{children}</div>;}
export function TabsTrigger({value,children,className='',...props}:{value:string;children:React.ReactNode;className?:string}&React.ButtonHTMLAttributes<HTMLButtonElement>){const ctx=React.useContext(Ctx)!;const active=ctx.value===value;return <button onClick={()=>ctx.setValue(value)} className={`px-3 py-1.5 text-sm rounded-md border ${active?'bg-black text-white border-black':'bg-white text-slate-700 border-slate-300'} ${className}`} {...props}>{children}</button>;}
export function TabsContent({value,children,className='' }:{value:string;children:React.ReactNode;className?:string}){const ctx=React.useContext(Ctx)!;if(ctx.value!==value)return null;return <div className={className}>{children}</div>;}
